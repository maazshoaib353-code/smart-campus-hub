import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.maaz.smartcampushub',
  appName: 'Smart Campus Hub',
  webDir: 'build'
};

export default config;
